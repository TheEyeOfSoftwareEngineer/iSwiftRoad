//
//  Chapeter2-Challenges.swift
//  LearningSwift
//
//  Created by yao on 30/5/21.
//

import Foundation

// MARK: Coordinates
let Coordinates: (Int, Int) = (2, 3)

// MARK: Named coordinate
let namedCoordinate: (Int, Int) = (row: 2, column: 3)

// MARK: Which are valid?
let character: Character = "⚽️"
let string: String = "Dog"

// MARK: Does it compile?
let tuple = (day: 15, month: 8, year: 2015)
let day = tuple.Day

// MARK: Find the error
let name = "Matt"   // constants
name += " Galloway"

// MARK: What is the type of value?
let tuple = (100, 1.5, 10)
let value = tuple.1 // Doouble

// MARK: what is the value of month?
let tuple = (day: 15, month: 8, year: 2015)
let month = tuple.month // 8

// MARK: What is the value of summary?
let number = 10
let multiplier = 5
let summary = "\(number) multiplied by \(multiplier) equals \(number * multiplier)"
// "10 multiplied by 5 equals 50"

// MARK: Compute the value
let a = 4
let b: Int32 = 100
let c: UInt8 = 12

// MARK: Diffenrent precision

